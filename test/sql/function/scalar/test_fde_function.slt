# Test FDE function with multi-dimensional array input
# Note: INSERT SELECT is not yet implemented, so we test with SELECT statements first

# Test FDE function with multi-dimensional array input
query I
SELECT FDE([[1.0, 2.0], [3.0, 4.0]], 64);
----

# Test FDE function with single array input
query I
SELECT FDE([1.0, 2.0, 3.0, 4.0], 64);
----

# Test FDE function with different target dimensions
query I
SELECT FDE([[0.0, -10.0, 0.0, 0.7], [9.2, 45.6, -55.8, 3.5]], 128);
----

# Test FDE function with smaller target dimension
query I
SELECT FDE([[1.0, 2.0], [3.0, 4.0]], 32);
----

# Test with existing tensor data (when available)
# This will be useful once INSERT SELECT is implemented

# Future usage examples (when INSERT SELECT is implemented):
# CREATE TABLE test_fde_table (col1 INT, col2 EMBEDDING(FLOAT,64));
# INSERT INTO test_fde_table SELECT 1, FDE([[1.0, 2.0], [3.0, 4.0]], 64);
# INSERT INTO test_fde_table SELECT 2, FDE([1.0, 2.0, 3.0, 4.0], 64);
