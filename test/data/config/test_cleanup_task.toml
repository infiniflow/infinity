[general]
version = "0.2.0"
timezone = "utc-8"

[network]
[log]

[storage]
# close auto cleanup task
cleanup_interval = "0s"
# close auto compact
compact_interval = "0s"

[buffer]
[wal]
#short checkpoint interval to allow quick cleanup
delta_checkpoint_interval = "1s"
[resource]