[general]
version = "0.6.3"
time_zone = "utc-8"

[network]
[log]
log_level = "critical"

[storage]
# close auto optimize
optimize_interval = "0s"
# close auto cleanup task
cleanup_interval = "0s"
# close auto compaction
compact_interval = "0s"

[buffer]
[wal]
# close checkpoint
checkpoint_interval = "0s"

[resource]
