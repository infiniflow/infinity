[general]
version = "0.6.1"
time_zone = "utc-8"

[network]
[log]
log_to_stdout = true
log_level = "trace"

[storage]
persistence_dir = ""
optimize_interval = "0s"
cleanup_interval = "0s"
compact_interval = "0s"

[buffer]
memindex_memory_quota   = "100MB"
[wal]
checkpoint_interval = "1s"

[resource]
