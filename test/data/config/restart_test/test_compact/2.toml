[general]
version = "0.6.0"
time_zone = "utc-8"

[network]
[log]
log_to_stdout = true
log_level = "trace"

[storage]
data_dir = "/var/infinity/data"
optimize_interval = "0s"
cleanup_interval = "2s"
compact_interval = "1s"
persistence_dir = ""

[buffer]
[wal]
full_checkpoint_interval = "3s"

[resource]
