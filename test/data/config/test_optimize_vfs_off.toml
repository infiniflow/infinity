[general]
version = "0.5.0"
time_zone = "utc-8"

[network]
[log]

[storage]
data_dir = "/var/infinity/data"
# close auto optimize
optimize_interval = "0s"
# close auto compaction
compact_interval = "0s"

[wal]
[buffer]
[resource]
